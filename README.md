# ideabreed preliminary task as assigned 

## built using react-native-cli and a couple of third party dependency package .

run "npm install" and "npx react-native run-android" in your terminal to run the project